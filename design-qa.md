# 模板设计验收

## 对照对象

- 设计图：
  - `/Users/andy/.codex/generated_images/019f51d2-ddca-7ec0-bee0-3826bb9db473/exec-2749e44b-f905-409c-a546-84c4c3363ca4.png`（账本）
  - `/Users/andy/.codex/generated_images/019f51d2-ddca-7ec0-bee0-3826bb9db473/exec-7d00af13-12b3-4931-8dd6-1fe92b935459.png`（沉静）
  - `/Users/andy/.codex/generated_images/019f51d2-ddca-7ec0-bee0-3826bb9db473/exec-9eafe826-095c-4b27-83f6-5f2db513abaa.png`（蓝图）
- 实现截图：`/tmp/resumer-design-qa/{ledger,authority,blueprint}.png`，由 Puppeteer 生成的 A4 PDF 首页栅格化得到。
- 全页对照：`/tmp/resumer-design-qa/{ledger,authority,blueprint}-comparison.png`。
- 局部对照：`/tmp/resumer-design-qa/{ledger,authority,blueprint}-header-comparison.png`。
- 浏览器状态：本地开发预览，默认简历内容、已上传测试肖像、三套模板均可选择；浏览器控制台无 error。
- 视口：A4 PDF 595 × 842 px 栅格化；浏览器桌面预览用于模板切换与配色交互验证。

## 首轮发现与修正

- [P2] 账本模板缺少设计图中的纵向节点锚点。
  - 修正：为编号栏补充虚线时间轴和朱红节点，保持跨页时正文可自然分页。
- [P2] 切换模板后会沿用前一个模板的主题变量，新模板默认色被覆盖。
  - 修正：切换模板时同步加载该模板的默认主题变量；账本、沉静、蓝图分别恢复氧化朱红、森林铜和工程蓝。
- [P2] 蓝图模板的三列联系方式在浏览器预览中可能换行。
  - 修正：按内容长度调整列宽、缩小辅助字号并保持单行显示。

## 复验结果

- 字体与层级：账本使用大号无衬线姓名和等宽元数据；沉静使用衬线姓名与低对比正文；蓝图使用技术标签和结构化栏位。中文正文在 PDF 和浏览器中均可读。
- 间距与布局：三套模板均保持 A4 页面边界和稳定分区。账本、沉静与蓝图分别保留侧栏、编辑式主栏、双栏技术文档三种信息层级。
- 颜色与令牌：每个模板默认使用完整的纸张、正文、主色、副色组合；配色面板的 8 个预置会同时更新背景、正文、主色和副色。浏览器中“铜绿”预置已验证为 `#fbf8f1` 背景和 `#202725` 正文色。
- 图片与图标：模板对用户照片使用统一的裁切规则；蓝图联系方式复用项目既有图标组件，无占位图或手绘图标。
- 文案与数据：正文继续直接来自 YAML Frontmatter 与 Markdown。设计图的示例经历比默认测试简历更长，因此页面填充率不同；这是内容量差异，不是布局溢出。
- 交互与可用性：模板选择器可切换 10 套模板；点击配色预置后活动状态与页面计算样式均正确更新。

## 后续抛光

- [P3] 模板数增至 10 后，窄桌面窗口中的模板选择器需要横向滚动；当前会保留选中模板可见，后续可考虑增加分类或折叠入口。
- [P3] PDF 依赖本机字体回退；若未来要做跨机器的完全一致输出，可再打包本地字体文件。

final result: passed
